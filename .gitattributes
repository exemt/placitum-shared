# Копия корневого правила платформы: в поставочной ветке этот каталог
# становится корнем, и корневого .gitattributes рядом нет. Без него клон на
# Windows получает CRLF, а sh-скрипты в образе ломаются невнятно.
* text=auto eol=lf
